name: Build and Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "yarn"

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: yarn install --immutable

      - name: Build shared package
        run: yarn workspace @flow/shared build

      - name: Build transpiler package
        run: yarn workspace @flow/transpiler build

      - name: Build frontend
        run: yarn workspace @flow/frontend build

      - name: Run tests
        run: yarn test
        continue-on-error: true

      - name: Copy frontend to custom_components
        run: |
          rm -rf custom_components/flow/www/*
          cp -r packages/frontend/dist/* custom_components/flow/www/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: flow
          path: custom_components/flow/
          retention-days: 30
